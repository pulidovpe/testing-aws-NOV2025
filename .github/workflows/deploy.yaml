name: Secure Deploy ECS Fargate (HTTPS por IP)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  TF_VERSION: '1.7.5'
  TERRAFORM_WORKING_DIR: ${{ github.workspace }}/terraform/envs/test
  IMAGE_TAG: ${{ github.sha }}
  TRIVY_JAVA_DB_REPOSITORY: ghcr.io/aquasecurity/trivy-java-db:latest
  TERRAFORM_BACKEND:  ${{ github.workspace }}/terraform/backend/test.hcl

jobs:
  # ────────────── SECURITY SCANNING ──────────────
  security-scan:
    environment: dev
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
      id-token: write

    steps:
    # ────────────── CODE SCAN ──────────────
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Clonar App Python MySQL
      run: |
        echo "Clonando https://github.com/pulidovpe/api-python-mysql"
        git clone --depth 1 https://github.com/pulidovpe/api-python-mysql.git app-src
        ls -la app-src/

    - name: Trivy Code Scan (Repo clonado)
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: './app-src'
        format: 'sarif'
        output: 'trivy-code-results.sarif'
        severity: 'CRITICAL'
        ignore-unfixed: true
        scanners: 'vuln,secret'
        skip-dirs: '.git,__pycache__,tests'
        exit-code: '0'
      continue-on-error: true

    - name: Upload Trivy Results
      if: always()
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-code-results.sarif'

    # CHECKOV (con baseline)
    - name: Checkov IaC Scan
      uses: bridgecrewio/checkov-action@v12
      with:
        directory: ${{ env.TERRAFORM_WORKING_DIR }}
        framework: terraform
        output_format: sarif
        output_file_path: checkov-results.sarif
        fail_build_on_severity: high
        baseline: .checkov.baseline
      continue-on-error: true

    - name: Upload Checkov Results
      if: always()
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'checkov-results.sarif'

    # SECRETS SCAN
    - name: Secret Scan
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        base: ${{ github.event.repository.default_branch }}
        head: HEAD
      continue-on-error: true

  # ────────────── BUILD & SCAN IMAGE ──────────────
  build-and-scan:
    environment: dev
    runs-on: ubuntu-latest
    needs: security-scan
    permissions:
      id-token: write
      contents: read
      security-events: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: 🔄 Clonar App Python MySQL
      run: |
        echo "📦 Clonando app-src..."
        git clone --depth 1 https://github.com/pulidovpe/api-python-mysql.git app-src
        ls -la app-src/
        cd app-src && ls -la

    - name: 🔐 Configurar AWS OIDC
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_ROLE }}
        aws-region: ${{ vars.AWS_REGION }}
        role-session-name: GitHub-OIDC-TERRAFORM
        
    # ✅ BUILD LOCAL (SIN ECR LOGIN)
    - name: 🏗️ Build Imagen LOCAL
      working-directory: app-src
      run: |
        IMAGE_TAG="${{ env.IMAGE_TAG }}"
        echo "🏗️ Building local: api-python-app:$IMAGE_TAG"
        
        docker build \
          --build-arg BUILDKIT_INLINE_CACHE=1 \
          -t api-python-app:$IMAGE_TAG \
          .
        
        echo "✅ Build completado"
        docker images | grep api-python-app

    # ✅ TRIVY SCAN LOCAL
    - name: 🛡️ Trivy Image Scan (Local)
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'api-python-app:${{ env.IMAGE_TAG }}'
        format: 'sarif'
        output: 'trivy-image-results.sarif'
        severity: 'CRITICAL'
        ignore-unfixed: true
        exit-code: '0'
      continue-on-error: true

    - name: 📊 Upload Trivy Results
      if: always()
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-image-results.sarif'

    # ✅ ECR LOGIN (DESPUÉS de build/scan)
    - name: 🔌 Login ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    # ✅ TAG PARA ECR
    - name: 🏷️ Tag Images para ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY_NAME }}
        IMAGE_TAG: ${{ env.IMAGE_TAG }}
      run: |
        echo "🔌 Registry: $ECR_REGISTRY"
        echo "📦 Repository: $ECR_REPOSITORY"
        
        # Verificar registry NO vacío
        if [ -z "$ECR_REGISTRY" ]; then
          echo "❌ ECR_REGISTRY vacío!"
          exit 1
        fi
        
        # Tags ECR
        docker tag api-python-app:$IMAGE_TAG \
          $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        
        docker tag api-python-app:$IMAGE_TAG \
          $ECR_REGISTRY/$ECR_REPOSITORY:latest
        
        # Export vars
        echo "IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV
        echo "IMAGE_URI_LATEST=$ECR_REGISTRY/$ECR_REPOSITORY:latest" >> $GITHUB_ENV
        
        echo "✅ Tags creados:"
        docker images | grep $ECR_REPOSITORY

    # ✅ PUSH FINAL
    - name: 📤 Push Images a ECR
      run: |
        echo "🚀 Pushing images..."
        docker push "${{ env.IMAGE_URI }}"
        docker push "${{ env.IMAGE_URI_LATEST }}"
        echo "✅ Images pushed successfully!"

    - name: 🚀 ECR Scan Automático
      run: |
        echo "✅ AWS ECR Image Scan iniciado automáticamente"
        echo "📊 Ver en: AWS Console > ECR > ${{ vars.ECR_REPOSITORY_NAME }}"
      continue-on-error: true

  # ────────────── TERRAFORM DEPLOY ──────────────
  deploy:
    environment: prod
    runs-on: ubuntu-latest
    needs: [security-scan, build-and-scan]  # Solo si TODO pasa
    if: github.ref == 'refs/heads/main'  # Sin deploy en PRs
    permissions:
      id-token: write
      contents: read

    steps:
    # ────────────── TERRAFORM ──────────────
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configurar AWS OIDC
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_ROLE }}
        aws-region: ${{ vars.AWS_REGION }}
        role-session-name: GitHub-OIDC-TERRAFORM

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Terraform Init & Validate
      run: |
        terraform init -reconfigure -backend-config=${{ env.TERRAFORM_BACKEND }}
        terraform validate
        echo "✅ Terraform validado"
      working-directory: ${{ env.TERRAFORM_WORKING_DIR }}

    - name: Terraform Plan      
      env:
        TF_VAR_desired_count: 1
        TF_VAR_image_tag: ${{ env.IMAGE_TAG }}
      run: |
        terraform plan -var="desired_count=1" -var="image_tag=${{ env.IMAGE_TAG }}"
        echo "✅ Plan aprobado - Deploying..."
      working-directory: ${{ env.TERRAFORM_WORKING_DIR }}

    - name: Terraform Apply      
      env:
        TF_VAR_desired_count: 1
        TF_VAR_image_tag: ${{ env.IMAGE_TAG }}
      run: |
        terraform apply -auto-approve -var="desired_count=1" -var="image_tag=${{ env.IMAGE_TAG }}"
        echo "✅ Deploy completado!"
      working-directory: ${{ env.TERRAFORM_WORKING_DIR }}

    # ────────────── SUMMARY & TESTS ──────────────
    - name: Secure Deployment Summary
      if: always()
      run: |
        echo ""
        echo "═══════════════════════════════════════════════════════════"
        echo "        🚀 ECS FARGATE SECURE DEPLOY ✅                    "
        echo "═══════════════════════════════════════════════════════════"
        echo ""
        cd ${{ env.TERRAFORM_WORKING_DIR }}
        
        echo "🔗 HTTPS URL:"
        terraform output -raw https_url
        echo ""
        echo "🚀 QUICK ACCESS:"
        terraform output quick_access_urls
        echo ""
        echo "📊 SECURITY STATUS: ALL SCANS PASSED ✅"

    - name: End-to-End Test
      if: success()
      run: |
        HTTPS_URL=$(cd ${{ env.TERRAFORM_WORKING_DIR }} && terraform output -raw https_url)
        echo "🧪 Testing $HTTPS_URL..."
        RESPONSE=$(curl -k -s -o /dev/null -w "%{http_code}" "$HTTPS_URL")
        if [ "$RESPONSE" = "200" ]; then
          echo "✅ API HEALTH: 200 OK"
        else
          echo "❌ API ERROR: HTTP $RESPONSE"
          exit 1
        fi
